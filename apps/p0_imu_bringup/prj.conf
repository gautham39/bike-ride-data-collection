CONFIG_PRINTK=y
CONFIG_CBPRINTF_FP_SUPPORT=y

CONFIG_I2C=y
CONFIG_GPIO=y

# We drive the chip ourselves — keep the stock driver off the bus.
# It has `default y`, so this must be explicit.
CONFIG_LSM6DSL=n

